{
  "name": "js-mentor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client"
  },
  "dependencies": {}
}
