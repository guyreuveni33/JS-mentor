{
  "name": "js-mentor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix server"
  },
  "dependencies": {
    "netlify-lambda": "^1.6.3"
  }
}
